# express-api

## END POINTS

# Create a new Thing

### Request

`POST api/addmusic/`

    body: {
        "name": "string",
        "writer": "string",
        "singer": "string",
        "genre": "string"
    }

### Response
    200 Ok





### Installation
1. `docker compose build`
2. `docker compose up -d`